target=wait$(EXEEXT)
objs=wait.o wait-rc$(RESEXT)

all: $(target)

$(target): $(objs)
	$(LD) $(LDFLAGS) $(objs) $(LDLIBS) -o $@

wait-rc$(RESEXT): wait.rc
	$(RC) $< -o $@

clean:
	-rm $(target) $(objs)
